name: Build APK
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unzip project
        run: unzip -o FlappyWings.zip
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - run: chmod +x FlappyWings/gradlew || true
      - run: cd FlappyWings && gradle wrapper --gradle-version 8.4
      - run: cd FlappyWings && ./gradlew assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: FlappyWings/app/build/outputs/apk/debug/app-debug.apk
